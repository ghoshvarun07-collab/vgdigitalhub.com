const homeLink=document.querySelector('.brand')?.getAttribute('href')||'index.html';const rootPrefix=homeLink.replace(/index\.html$/,'');const pageUrl=path=>rootPrefix+path.replace(/^\//,'').replace(/\/$/,'/index.html');
const navToggle=document.querySelector('[data-nav-toggle]');const nav=document.querySelector('[data-nav]');if(navToggle&&nav){navToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});}
const setScrolled=()=>document.body.classList.toggle('is-scrolled',window.scrollY>360);setScrolled();window.addEventListener('scroll',setScrolled,{passive:true});
document.querySelectorAll('[data-form]').forEach(form=>{form.addEventListener('submit',event=>{event.preventDefault();const status=form.querySelector('.form-status');if(!form.checkValidity()){form.reportValidity();return;}if(status)status.textContent='Thanks. Redirecting you now...';setTimeout(()=>{window.location.href=pageUrl('/thank-you/');},500);});});
const search=document.querySelector('[data-blog-search]');const posts=[...document.querySelectorAll('[data-posts] article')];if(search){search.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();posts.forEach(p=>p.hidden=q&&!p.dataset.title.includes(q));});}
document.querySelectorAll('[data-category]').forEach(btn=>btn.addEventListener('click',()=>{const cat=btn.dataset.category;posts.forEach(p=>p.hidden=p.dataset.cat!==cat);}));
const redirect=document.querySelector('.search-redirect');if(redirect){redirect.addEventListener('submit',e=>{e.preventDefault();const q=redirect.querySelector('input').value.toLowerCase();window.location.href=pageUrl(q.includes('seo')?'/seo-services/':q.includes('ads')||q.includes('ppc')?'/google-ads-services/':q.includes('web')?'/website-development/':'/services/');});}